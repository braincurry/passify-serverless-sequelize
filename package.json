{
  "name": "serverless-sequelize",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "local": "serverless offline start --stage local",
    "migrate": "stage=local npx sequelize db:migrate --env local",
    "undo-migrate": "stage=local npx sequelize db:migrate:undo --env local"
  },
  "license": "MIT",
  "dependencies": {
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.21.4",
    "serverless-http": "^2.7.0",
    "serverless-sequelize-migrations": "^1.1.1"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.2",
    "serverless-offline": "^12.0.3"
  }
}
